// Deprecated file – replaced by CardUI.Manager.Core and Controllers
// This stub intentionally does nothing. ui_manager.js is no longer used.
// It is kept only as a placeholder to avoid 404s if some stale HTML still references it.
// Safe to remove after all pages drop the script include.
(function(){ try { console && console.debug && console.debug('[ui_manager] deprecated; no-op stub loaded'); } catch(_){} })();
